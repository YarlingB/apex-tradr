import { View } from 'react-native';
import { Typography } from '@shared/components/ui';

const WatchlistCards = () => {
  return (
    <View>
      <Typography variant="lg">Watchlist Cards</Typography>
    </View>
  );
};

export default WatchlistCards;
